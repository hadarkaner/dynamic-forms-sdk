# Database

The PostgreSQL schema, migrations, and Prisma client live in [backend/prisma](../backend/prisma). This folder is reserved for standalone database assets (e.g. seed data, ER diagrams) that aren't tied to the backend's Prisma setup.
